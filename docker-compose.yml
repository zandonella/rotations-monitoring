services:
    monitor:
        build: .
        container_name: rotations-monitor
        restart: unless-stopped
        env_file: .env
        ports:
            - '8080:8080'
        volumes:
            - ./data:/app/data
